// Haiku Cosmos card visuals scaffold.
// Card drawing extraction from cards.js belongs in a later implementation pass.
(function (root) {
  "use strict";

  root.HC = root.HC || {};
  root.HC.CardVisuals = root.HC.CardVisuals || {};
})(typeof window !== "undefined" ? window : globalThis);
