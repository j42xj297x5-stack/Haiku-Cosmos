# Legacy runtime

Ten katalog przechowuje historyczne pliki wejściowe/runtime i nie jest źródłem prawdy.

- `index.codex.html` — dawny równoległy entrypoint z root repozytorium, przeniesiony tutaj jako materiał historyczny. Aktywnym entrypointem gry jest root `index.html`.
