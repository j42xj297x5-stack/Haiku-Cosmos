# docs/legacy

Ten katalog nie jest źródłem prawdy.

## Podkatalogi

- `systems/` — historyczne dokumenty systemowe (np. dawny `TARGETS_SYSTEM`).
- `technical/` — stare mapy techniczne i dokumenty narzędziowe.
- `runtime/` — historyczne pliki wejściowe/runtime; nie są aktywnym źródłem prawdy.
- `workflow/` — historyczne instrukcje workflow dla Codexa.
- `tools/` — miejsce na historyczne dokumenty narzędziowe.
- `ui/` — historyczne/zastąpione dokumenty UI (np. zastąpione przez nowszy master spec).
- `unsorted/` — dokumenty niesklasyfikowane (DO PRZEJRZENIA), jeśli pojawią się w kolejnych migracjach.

## Zasady

- Trafiają tu dokumenty nieaktualne, zastąpione lub sprzeczne z aktualnym stanem projektu.
- Dokumenty legacy czytamy wyłącznie jako kontekst historyczny.


## Ostatnie migracje

- `ui/SUB_META_V2_LAYOUT_SPEC.md` — przeniesiony z `docs/current/ui/` jako zastąpiony przez `SUB_META_V2_MASTER_SPEC.md` po audytach 6A/6B.
- `ui/SUB_META_V2_WIREFRAME_SPEC.md` — przeniesiony z `docs/current/ui/` jako zastąpiony przez `SUB_META_V2_MASTER_SPEC.md` po audytach 6A/6B.

- `runtime/index.codex.html` — przeniesiony z root jako historyczny/legacy entrypoint; aktywnym entrypointem gry jest `index.html`.
